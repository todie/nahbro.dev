export function generateAiTxt(): string {
  return [
    "# nahbro.dev — nah bro as a service",
    "# https://nahbro.dev",
    "",
    "# nah.",
    "",
    "User-agent: *",
    "Disallow: /",
  ].join("\n");
}
